//use an array (mock data or pull from API) and map through to display multiple shop items
//and render a ShopItem component for each item
const ProductItems = () => {
  return (
    <div>ShopItems</div>
  )
}
export default ProductItems