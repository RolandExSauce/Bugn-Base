//layout to hold product items (will render ProductItems components) and possibly filters/sorting options
//Can only be used for UserOrders view ? 
const ProductBoard = () => {
  return (
    <div>ProductBoard</div>
  )
}
export default ProductBoard